# CV

The content is sourced from `INFO.md` and reproduced in `cv.tex`.

## Files
- `cv.tex` - LaTeX source for the CV.
- `Makefile` - targets to build and clean the PDF.
- `INFO.md` - structured data.

## Build

- Build the PDF: `make`
- Clean artifacts: `make clean`
